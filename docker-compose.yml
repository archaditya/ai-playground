version: "3.9"

services:
  ai-playground:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-engineering-playground
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
